package <%= entityClass %>Request

type UpdateRequestDTO struct {
  // Auto-gen fields<% fields.forEach(function(field){ %>
  <%= field.name %> <%= field.type %> <% if (field.jsonConfig !== '') {%>`<%- field.jsonConfig %>`<%}%><% }); %>
  // Auto-gen fields : dont remove
}
